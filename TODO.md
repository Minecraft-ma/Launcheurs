# TODO — Rend mon UI Meilleur (UX)

- [ ] Mettre à jour `renderer.js` : attacher les listeners IPC PLAY une seule fois (anti-multiples handlers)
- [ ] Mettre à jour `renderer.js` : log limité (ex: 250 lignes) + meilleure méthode d’append + autoscroll conditionnel
- [ ] Mettre à jour `renderer.js` : états UI pendant “Installer mods” et “Lancement” (désactiver inputs, changer texte boutons)
- [ ] Mettre à jour `index.html` : améliorer style de `logOutput` + ajouter petite méta info de progression/spinner si nécessaire
- [ ] Tester manuellement : lancer PLAY (ne doit pas dupliquer les logs/progress), installer mods (progress OK)

